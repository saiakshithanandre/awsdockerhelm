apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws_docker.fullname" . }}-test-connection"
  labels:
    {{- include "aws_docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws_docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
